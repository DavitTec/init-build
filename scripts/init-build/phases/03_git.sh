#!/usr/bin/env bash
# Part of init-build library
# TODO: fix the issue incorrest formating, USE gitignore.sh with OPTIONS
#  
gitignore   # check for gitignore file and install if missing
git_init    # Git Initialise
git_first_commit  # Commit